run the code in the directory to download necessary csv files.

The data is hourly consumption data for most hours in a span of 15 years.

The code will process data and compute variance eigen values and vectors for the hourly consumption over a 24 hour periods.

Principal components are computed and a low dimensional representation of the data is produced with top two components.

The code works and is under construction.

  